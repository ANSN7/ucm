apiVersion: v1
kind: PersistentVolume
metadata:
  name: ucm-pv-volume1
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 10Gi
  accessModes:
  - ReadWriteOnce
  hostPath:
    path: "/mnt/ucmdata"
